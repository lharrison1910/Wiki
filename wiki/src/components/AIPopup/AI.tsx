import { TextField } from "@mui/material";

function AI() {
  return (
    <div className="h-16 w-40 bg-white shadow-md">
      <div>
        I need an array of some kind to determine show converstation history.
        What is a good way to do that? Didn't i do this for Horizon?
      </div>
      <TextField />
    </div>
  );
}

export default AI;
